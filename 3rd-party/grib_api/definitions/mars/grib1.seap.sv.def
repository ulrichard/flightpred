alias mars.number = forecastOrSingularVectorNumber;
alias mars.origin = identificationOfOriginatingGeneratingCentre;

# For TOST
if(class == 9) { alias mars.number  = forecastOrSingularVectorNumber; alias mars.method = methodNumber; }
